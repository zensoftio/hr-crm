upstream web {
  ip_hash;
  server web:8000;
}

server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass http://web/;
        proxy_set_header Host $http_host;
    }

    location /static/ {
        autoindex on;
        alias /static/;
    }
}