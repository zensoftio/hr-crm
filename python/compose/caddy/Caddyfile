{$DOMAIN_NAME}:3000 {
    proxy / react:3000 {
    }
    log stdout
    errors stdout
    gzip
}

{$DOMAIN_NAME} {
    root /app
    proxy / django:5000 {
        transparent
        except /static
        header_upstream Host {host}
        header_upstream X-Real-IP {remote}
        header_upstream X-Forwarded-Proto {scheme}
    }
    log stdout
    errors stdout
    gzip
}