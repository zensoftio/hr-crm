import React, { Component } from 'react';
import { withStyles } from '@material-ui/core/styles';

class TemplateContainer extends Component {
  constructor(props) {
    super(props)
  }
}
export default withStyles(TemplateContainer);
